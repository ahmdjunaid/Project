<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
        }

        .error-container {
            display: flex;
            max-width: 800px;
            width: 100%;
            background: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .brand-section {
            flex: 1;
            background: #000;
            padding: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo {
            width: 200px;
            height: auto;
        }

        .content-section {
            flex: 1;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .error-code {
            font-size: 72px;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .error-code svg {
            width: 40px;
            height: 40px;
        }

        .error-message {
            font-size: 16px;
            color: #333;
            margin-bottom: 20px;
        }

        .back-link {
            color: #0066cc;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .decorative-leaf {
            position: absolute;
            right: 0;
            bottom: 0;
            width: 150px;
            height: 150px;
            opacity: 0.5;
            pointer-events: none;
        }

        @media (max-width: 768px) {
            .error-container {
                flex-direction: column;
            }

            .brand-section,
            .content-section {
                padding: 30px;
            }

            .error-code {
                font-size: 56px;
            }

            .decorative-leaf {
                width: 100px;
                height: 100px;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="brand-section">
            <img src="/images/DialogueLogo.png" alt="Dialogue Digital Gallery" class="logo">
        </div>
        <div class="content-section">
            <div class="error-code">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/>
                    <line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/>
                </svg>
                404
            </div>
            <p class="error-message">
                Page not found. Check the address or <a href="/" class="back-link">go back</a>.
            </p>
        </div>
    </div>
    
    <svg class="decorative-leaf" viewBox="0 0 100 100" fill="#2F4F4F">
        <path d="M90,50 Q70,20 50,50 T10,50 Q30,80 50,50 T90,50"/>
    </svg>
</body>
</html>